// SublimeLinter Settings - User
{
	"delay": 1.0,
	"linters":
    {
        "gcc": {
            "disable": false,
            "executable": ["gcc"],
            // "args": ["-fsyntax-only", "-std=c90"],
            // "args": ["-Wall", "-Werror", "-Wextra"],
            "I": [
            	"${file_path}",
                "${file_path}/include",
                "${folder}/include",
                "/usr/local/include",
            ],
            "excludes": [],
        },
    }
}
